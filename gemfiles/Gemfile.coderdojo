# to use this gemfile, set BUNDLE_GEMFILE:
# export BUNDLE_GEMFILE=./gemfiles/Gemfile.coderdojo

theme_url = 'https://github.com/learnery/learnery-theme-coderdojo.git'
eval(File.read('Gemfile'),binding)
