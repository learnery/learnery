
   <% if @topic.send(attribute) %>
   <div class = "control_group">
     <%= hidden_field(:topic, "#{attribute}_id".to_sym) %>
     <%= t("activerecord.attributes.learnery/topic.#{attribute}")%>: <%=@topic.send(attribute).name%>
   </div>
   <% else %>
     <%= form.text_field attribute.to_sym  %>
   <% end %>


